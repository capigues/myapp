const button = document.querySelector('button')!;


button.addEventListener('click', () => {
    console.log('Clicked!');
});
/*
git checkout
git add .
git commit -m
git status
git clone
git diff
git merge
git pull - uses remote repository
git push - uses remote repository
git rebase 
git reset
git restore
git revert
git show - shows commits
git switch

*/